{
  "name": "rts-modularize",
  "private": true,
  "description": "Dev-only: the AST tool that splits apps/games/rts's monolith into ES modules. Never deployed (tools/ is excluded from the install walk).",
  "type": "commonjs",
  "dependencies": {
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.0"
  }
}
