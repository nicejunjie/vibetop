# vibetop-browser

Persistent Chromium via xpra HTML5 (`:99`), plus the second xpra display (`:98`)
behind the X11 Launcher's GUI apps.

Full documentation: [`docs/browser.md`](../docs/browser.md).
Cross-project overview: [`CLAUDE.md`](../CLAUDE.md).

(Kept as a short pointer so Claude Code's per-project auto-loading
still surfaces the right docs — the canonical content is one level up
to avoid drift between this and the sibling vibetop docs.)
